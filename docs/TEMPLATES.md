# Templates

The MVP ships notebook templates only.

Built-ins:

- `empty_notebook.py`
- `value_input.py`

Template validation is available through:

```bash
bulletjournal validate-templates
```

Templates are read-only sources copied into project notebooks when instantiated.
